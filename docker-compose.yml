version: '3'

services: 
    nginx_lb:
        build: ngnix
        ports: 
            - 8080:8080

    node1:
        build: .
        environment:
            - NODE_ENV=production
            - APP_ID=1
    node2:
        build: .
        environment:
            - APP_ID=2
    node3:
        build: .
        environment:
            - APP_ID=3